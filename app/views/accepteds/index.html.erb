
<%= render 'helpersLayout/header' %>

<section class="acceptedSection">
  <p id="notice"><%= notice %></p>
  <div class="groupAc">
    <div class="move_grupo">
      <h1 class="grupoT">GRUPOS</h1>
    </div>
    <div class="actionsG">
      <ul>
        <li class="displayFlexAction"><span class="icon-plus"></span><p><%= link_to 'Agregar', new_accepted_path %></p></li>
        <li class="displayFlexAction" id="loading"> <span class="icon-bin"></span><a href="/delateAll1" class="move_vaciar">Vaciar bases de datos</a></li>
        <li><span class="icon-address-book"></span><a href="#" class="move_revisarGrupos"> Revisar grupos </a>
          <ul>
            <li class="modi">
              <p id="ver_matutino" class="turnos">Turno Matutino</p>
            </li>
            <li>
              <p id="ver_vespertino" class="turnos">Turno Verpertino</p>
            </li>
          </ul>
        </li>

        <li>
          <%= form_tag(accepteds_path, :method => "get", id: "search-form") do %>
          <%= text_field_tag :search, params[:search], placeholder: "", id: "buscador" %>
          <%= submit_tag "Buscar", :name => nil, id: "buscadorBotton" %>
          <% end %>
        </li>
      </ul>
    </div>
  </div>

  <div class="renders">
    <% if Accepted.all.length > 0%>
      <%= render 'helpersLayout/groupsFinderA' %>
      <%= render 'helpersLayout/groupsLayoutA' %>
    <% end %>
  </div>
</section>
